<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.exam.mapper.MemberMapper">
	
	<!-- 회원가입 -->
	<insert id="memberAdd" parameterType="MemberDTO">
		insert into member (member_id,name,gender,access_pw,post,addr1,addr2, phone1, phone2, phone3, height,weight)
		values(#{member_id},#{name},#{gender},#{access_pw},#{post},#{addr1},#{addr2},#{phone1},#{phone2},#{phone3},#{height},#{weight})

	</insert>
	
	<select id="login" parameterType="hashmap" resultType="MemberDTO">
		select *
		from member
		where name=#{name} and access_pw=#{access_pw}
	</select>
	
	<!-- mypage -->
	<select id="mypage" parameterType="string" resultType="MemberDTO">
		select *
		from member
		where name=#{name}
	</select>
 
  
   
</mapper>


